{% extends "@layout/default.html.twig" %}
{% block content %}
    <div class="row">
        <div class="box">
            <div class="box-body">
                <a class="btn btn-app" href="{{ path('tag.create') }}">
                    <i class="fa fa-plus-circle"></i> Novo
                </a>
                <table class="table table-bordered">
                    <tbody>
                        <tr>
                            <th style="width: 10px;">#</th>
                            <th>Nome</th>
                            <th>Ações</th>
                        </tr>
                        {% for tag in tags %}
                            <tr>
                                <td>{{ tag.id }}</td>
                                <td>{{ tag.name }}</td>
                                <td><a href="{{ url('tag.update', {id: tag.id}) }}">Editar</a> |
                                <a href="{{ url('tag.delete', {id: tag.id}) }}">Excluir</a></td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}
    Listagem de Tags - {{ parent() }}
{% endblock %}

{% block page_header %}
    Listagem de Tags
{% endblock %}